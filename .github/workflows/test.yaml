name: Workflow test
on: push
jobs:
  Run-Test-Tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v3
      - name: Npm install
        run: npm install
      - name: Run Actual testcases
        run: npm test